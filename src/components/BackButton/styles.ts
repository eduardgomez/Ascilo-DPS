import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: "center"
  },
  text: {
    color: "#27B2B3",
    fontWeight: "800",
    fontSize: 17
  }
})