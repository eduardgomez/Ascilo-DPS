import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  loadingContainer: {
    flex: 1,
    justifyContent: 'center',
    alignContent: 'center',
    alignItems: 'center',
  },
  loadingText: {
    fontSize: 24,
    marginTop: 28,
    fontWeight: 'bold',
  },
})
